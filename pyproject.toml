[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.backends.legacy:build"

# ============================================================
# Métadonnées du projet
# ============================================================

[project]
name = "commonledger"
version = "0.7.7"
description = "Simplified accounting for non-profit associations — open source, accessible NVDA/JAWS"
readme = "README.md"
license = { file = "LICENSE" }
authors = [
    { name = "Amine Khettat", email = "amine.khettat@blindsystems.org" }
]
keywords = ["accounting", "nonprofit", "association", "accessibility", "nvda", "pdf", "french"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: End Users/Desktop",
    "Topic :: Office/Business :: Financial :: Accounting",
    "License :: Other/Proprietary License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Operating System :: Microsoft :: Windows",
    "Natural Language :: French",
]
requires-python = ">=3.11"
dependencies = [
    "pdfplumber>=0.10.0",
    "PyMuPDF>=1.23.0",
    "pandas>=2.1.0",
    "openpyxl>=3.1.0",
    "python-docx>=1.1.0",
    "matplotlib>=3.8.0",
    "comtypes>=1.2.0",
    "PySide6>=6.6.0",
    "Pillow>=10.0.0",
    "python-dateutil>=2.8.2",
]

[project.urls]
Homepage = "https://github.com/aminekhettat/commonledger"
Repository = "https://github.com/aminekhettat/commonledger"
Issues = "https://github.com/aminekhettat/commonledger/issues"

[project.scripts]
commonledger = "main:main"

# ============================================================
# bump-my-version — Gestion du versioning sémantique
# Usage :
#   bump-my-version bump patch   → 0.1.0 → 0.1.1  (correctif)
#   bump-my-version bump minor   → 0.1.0 → 0.2.0  (nouvelle fonctionnalité)
#   bump-my-version bump major   → 0.1.0 → 1.0.0  (rupture de compatibilité)
# ============================================================




















































[tool.bumpversion]
current_version = "0.1.0"
commit = true
tag = true
tag_name = "v{new_version}"
commit_message = "chore: bump version {current_version} → {new_version}"
allow_dirty = false

[[tool.bumpversion.files]]
filename = "pyproject.toml"
search = 'version = "{current_version}"'
replace = 'version = "{new_version}"'

[[tool.bumpversion.files]]
filename = "main.py"
search = 'setApplicationVersion("{current_version}")'
replace = 'setApplicationVersion("{new_version}")'

[[tool.bumpversion.files]]
filename = "README.md"
search = "CommonLedger v{current_version}"
replace = "CommonLedger v{new_version}"

[[tool.bumpversion.files]]
filename = "core/__init__.py"
search = '__version__ = "{current_version}"'
replace = '__version__ = "{new_version}"'

# ============================================================
# Outils de développement
# ============================================================

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "--strict-markers",
    "--tb=short",
    "-v",
    "--cov=core",
    "--cov-report=term-missing",
    "--cov-report=xml:coverage.xml",
    "--cov-report=html:htmlcov",
    "--cov-fail-under=100",
]
markers = [
    "integration: tests nécessitant des ressources externes (disque E, réseau)",
    "slow: tests lents (> 5 secondes)",
    "ui: tests de l'interface graphique PySide6",
]

[tool.coverage.run]
source = ["core"]
omit = [
    "*/tests/*",
    "*/ui/*",
    "*/__pycache__/*",
    "*/conftest.py",
    # Modules d'intégration pure (génération Word/PDF/graphiques)
    # Testés via tests d'intégration, pas en CI headless
    "core/reporter/docx_reporter.py",
    "core/reporter/graphiques.py",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "@abstractmethod",
]
show_missing = true
fail_under = 100  # couverture obligatoire à 100% — aucune ligne non testée acceptée

# ── Ruff (linter + formatteur) ────────────────────────────────────────────────
[tool.ruff]
line-length = 100
target-version = "py311"
src = ["core", "ui", "tests"]

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # isort
    "B",    # flake8-bugbear
    "C4",   # flake8-comprehensions
    "UP",   # pyupgrade
    "SIM",  # flake8-simplify
    "N",    # pep8-naming
    "ANN",  # flake8-annotations (type hints)
    "S",    # bandit (sécurité via ruff)
    "T20",  # no print statements in production
]
ignore = [
    # Annotations — trop verbeux pour un projet en phase alpha
    "ANN001",  # missing arg type annotation
    "ANN002",  # missing *args type annotation
    "ANN003",  # missing **kwargs type annotation
    "ANN201",  # missing return type (public)
    "ANN202",  # missing return type (private)
    "ANN204",  # missing return type (__init__, always None)
    "ANN206",  # missing return type (__new__)
    # Style
    "E501",    # line too long (géré par line-length)
    "E741",    # ambiguous variable name (l, O, I) — accepté dans les lambdas comptables
    "N806",    # variable in function should be lowercase (GUI vars)
    # Sécurité (acceptable dans ce contexte)
    "S101",    # assert usage (OK dans les tests)
    "S110",    # try-except-pass (OK pour des exceptions Word/LO non critiques)
    "S603",    # subprocess call (commandes hardcodées, pas d'input user)
    "T201",    # print statements (acceptables temporairement)
    # Simplifications optionnelles
    "SIM105",  # contextlib.suppress (style uniquement)
    "SIM117",  # single with (lisibilité des mocks de test)
    "B005",    # .strip() with multi-char (OK)
    "B007",    # loop variable not used (OK pour enumerate)
    "B905",    # zip without strict (Python 3.10+ only, ok pour 3.11+)
    "N813",    # camelcase alias (OK pour oxe = OxmlElement)
    "F841",    # local variable assigned but unused (OK dans matplotlib)
]
per-file-ignores = { "tests/**" = ["ANN", "S", "T20", "SIM"] }

[tool.ruff.lint.isort]
known-first-party = ["core", "ui"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "lf"

# ── MyPy (vérification de types) ─────────────────────────────────────────────
[tool.mypy]
python_version = "3.11"
# ── Mode strict activé sur core/ ─────────────────────────────────────────────
# mypy vérifie activement les types sur le code métier.
# Les règles ci-dessous activent les vérifications les plus importantes
# sans bloquer sur l'absence de stubs pour les bibliothèques tierces.
strict = false                     # strict global désactivé (active tout sans nuance)
# Activations explicites des règles critiques :
warn_return_any = true             # interdit les retours Any implicites
warn_unused_configs = true         # erreur si un override mypy ne sert à rien
warn_unused_ignores = true         # interdit les # type: ignore inutiles
warn_redundant_casts = true        # interdit les cast() superflus
no_implicit_optional = true        # Optional[X] doit être déclaré explicitement
check_untyped_defs = true          # vérifie les fonctions sans annotations
disallow_any_generics = true       # interdit List, Dict, Tuple sans paramètres de type
disallow_untyped_defs = true       # toute fonction de core/ doit être annotée
disallow_incomplete_defs = true    # pas d'annotations partielles
disallow_untyped_calls = false     # autorise les appels vers du code non annoté (PySide6)
ignore_missing_imports = true      # stubs absents pour pdfplumber/matplotlib/etc.
show_error_codes = true            # affiche les codes d'erreur pour les ignores ciblés
exclude = [
    "tests/",       # les tests n'ont pas besoin d'annotations complètes
    "ui/",          # PySide6 génère trop de faux positifs (slots, signals, etc.)
    "config/",
    "docs/",
    "scripts/",
]

[[tool.mypy.overrides]]
module = ["matplotlib.*", "PIL.*", "pdfplumber.*", "docx.*", "comtypes.*", "PySide6.*"]
ignore_missing_imports = true

# python-docx : stubs incomplets — Document n'est pas reconnu comme type valide.
# On désactive uniquement les codes d'erreur spécifiques aux stubs absents.
[[tool.mypy.overrides]]
module = ["core.reporter.docx_reporter"]
disable_error_code = ["no-untyped-def", "valid-type", "attr-defined", "no-any-return",
                      "assignment", "arg-type", "type-arg", "no-untyped-call"]

# matplotlib n'a pas de stubs complets pour les fonctions avancées
[[tool.mypy.overrides]]
module = ["core.reporter.graphiques"]
disable_error_code = ["no-untyped-def", "type-arg", "no-untyped-call", "no-any-return"]

# ── Bandit (sécurité) ─────────────────────────────────────────────────────────
# ── pdoc (génération de documentation API) ────────────────────────────────────
# Commande : pdoc --output-dir docs/api --docformat google core
# Résultat  : docs/api/core/*.html — documentation navigable et indexée
# Note      : docs/api/ est dans .gitignore (generé à chaque build CI)
#             La doc live sera publiée sur GitHub Pages via le workflow docs.yml
[tool.pdoc]
# Format des docstrings dans le code source
docformat = "google"

[tool.bandit]
exclude_dirs = ["tests", "ui", "config/assets", "scripts", "docs"]
# B101 : assert — justifié uniquement dans les tests (exclus ci-dessus)
# B324 : hashlib sans usedforsecurity=False → interdit sauf si explicitement justifié
# B603/B607 : subprocess avec liste de commandes hardcodées → autorisé dans scripts/
# Tous les autres problèmes de sécurité doivent lever une erreur
skips = []
severity = "low"     # tout problème même mineur doit être signalé
confidence = "low"   # signaler même les détections incertaines

# ── semantic-release (versioning automatique) ─────────────────────────────────
[tool.semantic_release]
version_toml = ["pyproject.toml:project.version"]
version_variables = [
    "main.py:CommonLedger.*0.1.0",
    "core/__init__.py:__version__",
]
branch = "main"
changelog_file = "CHANGELOG.md"
build_command = ""
upload_to_repository = false
upload_to_release = true
commit_message = "chore(release): v{version} [skip ci]"
tag_format = "v{version}"
major_on_zero = false   # La version majeure N'EST PAS auto-incrémentée
hvcs = "github"

[tool.semantic_release.commit_parser_options]
# Conventional Commits → version bump rules
# fix/chore/docs/style/refactor/test/perf/ci → PATCH
# feat → MINOR
# feat! ou BREAKING CHANGE → MAJOR (uniquement à la demande explicite)
allowed_tags = ["feat", "fix", "chore", "docs", "style", "refactor", "test", "perf", "ci", "build"]
minor_tags = ["feat"]
patch_tags = ["fix", "chore", "docs", "style", "refactor", "test", "perf", "ci", "build"]
